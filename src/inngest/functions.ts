// Register Inngest functions here and add them to the `functions` array below.
// They are served at /api/inngest (see src/app/api/inngest/route.ts).
//
// Example:
//
//   import { inngest } from "./client";
//
//   export const helloWorld = inngest.createFunction(
//     { id: "hello-world" },
//     { event: "demo/hello.world" },
//     async ({ event, step }) => {
//       return { message: `Hello ${event.data.name}` };
//     },
//   );

export const functions = [];
